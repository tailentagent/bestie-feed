# Bestie feed

Status feed + rendered videos for [@hey_bestie.app](https://www.instagram.com/hey_bestie.app) — the faceless page for the [Bestie app](https://girllogic.app).

- `joke-log.json` — one entry per day; the commit history is the monitoring feed.
- **Releases → renders** — the MP4s Buffer fetches at publish time. Do not delete.

The pipeline that generates these runs elsewhere.
